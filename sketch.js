function setup() {
    createCanvas(600, 400);
    background(240);
    textAlign(CENTER, CENTER);
    textSize(18);
    text("Coming soon: interactive market simulation!", width / 2, height / 2);
  }
  